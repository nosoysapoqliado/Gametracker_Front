// Endpoint HTTP del backend que el frontend usa para CRUD (servirá en http://localhost:3000/juegos)
export const API_URL = "http://localhost:3000/juegos";

// Nota: el frontend debe usar FALLBACK_API para las operaciones HTTP cuando isMongo === true.
export const REVIEWS_URL = "http://localhost:3000/resenas";
